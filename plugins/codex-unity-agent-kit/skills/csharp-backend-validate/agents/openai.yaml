interface:
  display_name: "C# Backend Validate"
  short_description: "Validate C# backend builds and tests"
  default_prompt: "Use $csharp-backend-validate to validate this C# backend change with the right build and test commands."
