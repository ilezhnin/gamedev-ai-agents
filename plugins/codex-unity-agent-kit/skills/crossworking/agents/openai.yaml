interface:
  display_name: "Crossworking"
  short_description: "Coordinate agents through a verified handoff"
  default_prompt: "Use $crossworking to coordinate implementation, simplification, validation, and independent review."
policy:
  # Natural-language teamwork requests may enter this safe handoff-only workflow.
  allow_implicit_invocation: true
