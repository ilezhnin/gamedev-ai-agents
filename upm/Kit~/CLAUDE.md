# Project Instructions

All project rules live in @AGENTS.md (imported into context by that reference) - follow it. Project contracts: [ARCHITECTURE.md](ARCHITECTURE.md), [CODE_STYLE.md](CODE_STYLE.md), [DEPENDENCIES.md](DEPENDENCIES.md).

Skills are installed natively under `.claude/skills/` (mirrored from `.agents/skills/` - same names, same content). Subagents live in `.claude/agents/`; permissions and hooks in `.claude/settings.json`. Project lessons live in `.agents/learnings.md`; active work state in `.agents/plans/`.
