interface:
  display_name: "Game Pipeline"
  short_description: "Run gated game milestones"
  default_prompt: "Use $game-pipeline to execute the approved game or feature GDD through verified milestones."
policy:
  # Natural-language requests enter safe Stage mode; milestone/auto still require explicit selection.
  allow_implicit_invocation: true
