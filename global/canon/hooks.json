{
  "comment": "Canonical automation hooks. Rendered into .codex/hooks.json (Codex) and the hooks block of .claude/settings.json (Claude Code) by the installers - never edit rendered files by hand. Scripts live in the platform-neutral .agents/scripts/ folder of the target project.",
  "hooks": [
    {
      "name": "unity-meta-check",
      "stacks": ["unity"],
      "event": "PostToolUse",
      "codexMatcher": "apply_patch",
      "claudeMatcher": "Edit|Write|MultiEdit",
      "command": "powershell -NoProfile -ExecutionPolicy Bypass -File .agents/scripts/check-unity-meta.ps1",
      "commandPwsh": "pwsh -NoProfile -File .agents/scripts/check-unity-meta.ps1"
    }
  ]
}
